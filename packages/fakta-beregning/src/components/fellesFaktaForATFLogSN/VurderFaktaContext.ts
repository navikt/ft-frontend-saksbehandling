import React from 'react';

export const GetErrorsContext = React.createContext(null);
export const BeregningsgrunnlagIndexContext = React.createContext(null);
const VurderFaktaContext = React.createContext(null);

export default VurderFaktaContext;
